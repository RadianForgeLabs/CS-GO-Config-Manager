<UserControl x:Class="CSGOConfigManager.Views.CommandBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <TextBlock DockPanel.Dock="Top" Text="Command Browser" Style="{StaticResource TitleText}"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="0,0,16,0"/>
            <TextBlock Text="Category:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding Category}" Width="140" Margin="0,0,16,0"/>
            <Button Content="Reload" Command="{Binding ReloadCommand}" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Commands}"
                      SelectedItem="{Binding Selected, Mode=OneWayToSource}" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Command" Binding="{Binding Name, Mode=OneWay}" Width="140"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category, Mode=OneWay}" Width="90"/>
                    <DataGridTextColumn Header="Default" Binding="{Binding DefaultValue, Mode=OneWay}" Width="70"/>
                    <DataGridTextColumn Header="Current" Binding="{Binding CurrentValue}" Width="70"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type, Mode=OneWay}" Width="60"/>
                    <DataGridTextColumn Header="Flags" Binding="{Binding FlagsDisplay, Mode=OneWay}" Width="90"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description, Mode=OneWay}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2" Style="{StaticResource Card}" Margin="0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top">
                        <TextBlock Text="Details" Style="{StaticResource SectionTitle}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding Selected.Name, Mode=OneWay}" FontWeight="SemiBold" FontSize="16"/>
                        <TextBlock Text="{Binding Selected.Description, Mode=OneWay}" TextWrapping="Wrap" Style="{StaticResource MutedText}" Margin="0,6,0,0"/>
                        <TextBlock Margin="0,8,0,0"><Run Text="File: "/><Run Text="{Binding Selected.File, Mode=OneWay}"/></TextBlock>
                        <TextBlock><Run Text="Modes: "/><Run Text="{Binding Selected.ModesDisplay, Mode=OneWay}"/></TextBlock>
                        <TextBlock><Run Text="Range: "/><Run Text="{Binding Selected.Min, Mode=OneWay}"/><Run Text=" – "/><Run Text="{Binding Selected.Max, Mode=OneWay}"/></TextBlock>

                        <TextBlock Text="Edit Value" Style="{StaticResource SectionTitle}"/>
                        <TextBox Text="{Binding EditValue, UpdateSourceTrigger=PropertyChanged}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Apply" Command="{Binding ApplyCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                            <Button Content="Reset Default" Command="{Binding ResetCommand}" Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>

                        <TextBlock Text="Conflict Sources" Style="{StaticResource SectionTitle}"/>
                    </StackPanel>
                    <TextBox Text="{Binding ConflictText}" IsReadOnly="True" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                             FontFamily="Consolas" FontSize="12"/>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
